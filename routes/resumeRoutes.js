import { Router } from 'express'; import { protect } from '../middleware/authMiddleware.js'; import { uploadResume } from '../middleware/uploadMiddleware.js'; import { upload,list,getOne } from '../controllers/resumeController.js'; const router=Router(); router.use(protect); router.route('/').get(list).post((req,res,next)=>uploadResume(req,res,e=>e?next(e):next()),upload); router.get('/:id',getOne); export default router;
