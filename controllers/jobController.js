import asyncHandler from 'express-async-handler'; import JobDescription from '../models/JobDescription.js'; import { indexDocument } from '../services/rag/ragService.js';
export const create = asyncHandler(async (req,res) => { const { title, company, content }=req.body; if(!content || content.trim().length<30){res.status(400);throw new Error('Enter a job description of at least 30 characters.');} const job=await JobDescription.create({user:req.user._id,title,company,content});try{await indexDocument({user:req.user._id,sourceType:'job',sourceId:job._id,text:content,metadata:{title:job.title||'Job description'}})}catch(error){console.warn(`Job RAG indexing unavailable: ${error.message}`)}res.status(201).json(job); });
export const list = asyncHandler(async (req,res) => res.json(await JobDescription.find({user:req.user._id}).sort('-createdAt')));
